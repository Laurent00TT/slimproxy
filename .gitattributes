# Normalize line endings in the repository to LF; let Go tooling and shells
# on every platform see consistent bytes. Windows checkouts still work -- Go,
# PowerShell, and git itself are all LF-tolerant.
* text=auto eol=lf
*.ps1 text eol=crlf
